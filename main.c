#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>

int main(int argc, char *argv[])
{

/*const int MAX =100, MIN =1;
int guess = 0;*/
int nombreMystere = 0 ;/*
int compteur = 0 ;

/*srand(time(NULL));
nombreMystere = (rand() % (MAX - MIN + 1)) + MIN;

printf("Devinez le nombre mystère compris entre 1 et 100 : ");
scanf("%d",&guess);

while(guess < nombreMystere)
{
printf("Nombre trop petit\nNouveau choix : ");
scanf("%d",&guess);
}
compteur =compteur+1 ;
while(guess > nombreMystere)
{
printf("Nombre trop grand\nNouveau choix : ");

scanf("%d",&guess);
}
compteur =compteur+1 ;
if (guess=nombreMystere)
{
printf("\nTrouvé !!!\nIl vous a fallu %d essais.",&compteur);
}*/
printf("%d",&nombreMystere);

  return 0;
}

